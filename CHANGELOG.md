# 更新日志

本插件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格记录；
版本号以 [`metadata.yaml`](./metadata.yaml) 为准，格式 `主.次.修订`。
括号内为对应 commit 的短哈希。

## [0.2.6] - 2026-09-25

- **放宽概率漏斗（修复"几乎不说话"）**：
  - `base_speak_probability` 默认 0.08 → 0.3；冷却默认 900/1800s → 180/600s。
  - 无关键词闲聊的兴趣倍率基线 0.35 → 0.6，词表外的话题不再被压到接近沉默。
  - 社交能量不再随普通聊天单调衰减（原先每条 -0.005 会把能量拖到 0.1 地板、概率锁死
    0.5%）；能量改为"发送消耗、命中兴趣话题补充、跨日重置回初始值"的电池模型。
  - 默认决策 Prompt 软化"大多数时候应该潜水"的措辞，气氛合适时允许自然插话。
- **决策漏斗日志**：每次决策在日志输出 `[decision]` 行——门控原因 / 无合适会话 / 得分 /
  概率 / 掷点（`-> evaluate` 或 `-> skip`）/ 决策结果（`-> IGNORE`、`-> speak` 等），
  一条日志即可定位"不说话"卡在哪一级。（同一 commit）
- **发送审计日志**：发送成功时记录目标 UMO 与回复内容，便于排查部分适配器
  （如 qq_official）返回 True 但静默丢消息的情况。（同一 commit）

## [0.2.5] - 2026-09-25

- **人格分离**：插件不再内置人格提示，移除 `persona_prompt` 配置项；主动回复的口吻自动跟随
  AstrBot 当前生效的人格（会话服务配置 > 会话人格 > 默认人格），由适配层解析后作为口吻参考
  拼入决策 system prompt。（7f8931b）
- 内置决策 Prompt 去除人格化措辞（"像普通群友" → 中性口语化要求），避免与 AstrBot 人格冲突。（7f8931b）
- 新增测试：决策 system prompt 不含人格内容。（7f8931b）

## [0.2.4] - 2026-09-25

- **修复安装失败**：AstrBot 构建插件默认配置时对 `object` 类型 schema 节点强制读取 `items`，
  导致安装报 `KeyError: 'items'`。`group_keyword_overrides` 改为 `list` 类型，
  每项格式 `UMO=关键词1,关键词2`；解析器同时兼容旧的字典格式与畸形行跳过。（566e365）

## [0.2.3] - 2026-09-25

- **修复插件无法加载**：适配 AstrBot 的包式加载（`data.plugins.<名字>.main`，插件目录不在
  `sys.path`），全部内部导入改为包相对导入，解决 `ModuleNotFoundError: No module named 'core'`；
  `state_store` 本地定义 `SCHEMA_VERSION` 并加同步测试。（c3e9928）
- **超时保护**：新增 `llm_timeout_seconds`（决策模型调用超时，0 = 不限制）；主动发送加 30 秒
  硬上限。Provider 挂起不再让群的决策槽永久卡死，超时按失败退避处理。（3301b12）
- **状态保存**：改为 dirty 标志排空循环，保存进行中产生的变更不再丢失。（3301b12）
- 决策 hint 与 `inject_group_context` 解耦：关闭注入只影响普通回复的群情注入，
  主动决策 prompt 始终携带群情提示。（1624efa）
- **引擎加固**：防抖触发增加在途决策重入保护；淘汰/重置群状态时同步清理 `last_decision`；
  白名单检查去掉每条消息一次的 `set()` 构建。（275c3f8）
- 回复最大长度可配置：新增 `max_reply_length`（默认 200，0 = 不限制）。（275c3f8）
- **非文本消息占位符增强**：新增 分享 / 卡片 / 转发消息 / 戳一戳 组件映射；未知组件回退通用
  `[消息]` 占位符不再丢弃；单条消息文本截断 500 字符，防止超长文本进入决策 prompt。（a9ad85b）
- **时区一致性**：会话 transcript 与 `/ws why` 显示的当前时间改用配置时区，与配额、
  静默时段保持一致。（7fd2cf8）
- `StateStore` 加载只读一次文件；`metadata.yaml` repo 指向实际仓库；README 补充完整安装说明。（29b8201 / c9997cd）

## [0.2.2] - 2026-09-25

- 代码评审修复：发送前按 `revision` 复检话题是否被切走；静默时段与跨日重置的时区处理；
  回应窗口按"与机器人相关"的消息结算；主动消息本地入库不依赖平台回显；
  决策模型失败独立退避（`llm_failure_backoff_seconds`）。（81b8a86）

## [0.2.1] - 2026-09-25

- **重连去重**：以 `umo:message_id` 为键、TTL + 容量上限的全局去重缓存作为管线第一道闸，
  服务重连重放的事件在进入采集 / 会话线程 / LLM 之前即被丢弃；
  无 `message_id` 时的内容指纹兜底默认关闭。（802b728）

## [0.2.0] - 2026-09-25

- **V2.0 会话线程 + 防抖决策**：观察到的消息按「引用 → @ → 关键词 → 同人续聊 → 新会话」
  聚类为会话线程；群聊刷屏时先静默等待（`debounce_seconds`，最长
  `debounce_max_wait_seconds`）再决策；每次只挑选一个最值得参与的会话做 LLM 判断，
  决策输出 `thread_id` 与 `target`（GROUP / USER）。（9911412）

## [0.1.1] - 2026-09-25

- 会话线程与防抖核心模块落地（V2.0 第一部分）：线程聚类、活跃度计算、话题识别与选择策略。（2af95d8）
- **流控体系**：每群 / 全局令牌桶突发限制、每群 / 全局每日主动上限、发送失败指数退避、
  时区感知的跨日重置、群状态 TTL 与数量上限淘汰。（374e39b）
- 设计文档（V2.0 会话线程 / 防抖 / 群级决策）移入本地 `docs/`（不随仓库发布）。（6b06f5e）

## [0.1.0] - 2026-09-25

- **首个可用版本（V1）**：
  - 群消息观察式采集（窗口、去重、速率桶），入口非文本消息归一为占位符；
  - 信号门控（冷却 / 刷屏 / 连续发言 / 静默时段 / 每日上限）与本地 SpeakScore 评分；
  - 三态 LLM 决策（`IGNORE` / `WAIT` / `SPEAK`），强制 JSON 输出与容错解析；
  - 分档冷却（普通 / 刚参与 / 连续发言 / 被忽略）、回应窗口、社交能量调节；
  - 主动消息写回 AstrBot 会话记忆；正常 LLM 请求注入群情动态上下文；
  - 管理指令 `/ws status|enable|disable|reset|why`（管理员）；
  - 状态原子持久化（临时文件 + `os.replace`）、`dry_run` 演练模式、群白名单默认关闭。（2e2abc2）
